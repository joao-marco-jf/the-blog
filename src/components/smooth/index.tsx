import SmoothContainer from "./smooth-container";
import SmoothFragment from "./smooth-fragment";
import SmoothSection from "./smooth-section";

const Smooth = {
    container: SmoothContainer,
    section: SmoothSection,
    fragment: SmoothFragment
}

export default Smooth;